# crate-paths-cli-core

The core logic library for the `crate-paths` CLI ecosystem.

This crate is intended for internal use by `crate-paths-cli` and related tools. It provides the implementation for:

- `Crates.io / Rustup / Local` source analysis.
- Path tree generation.
